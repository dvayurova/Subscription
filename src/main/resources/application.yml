spring:
  datasource:
    url: jdbc:postgresql://db:5432/subscription
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  logging:
    level:
      org.hibernate.SQL: DEBUG